return function(ctx)
    -- Function is intentionally kept as an independent module.
    -- Add/replace function cards here without touching FishHub_Main.lua.
    ctx.TabFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
end
